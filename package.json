{
  "name": "hive-cli",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "start": "powershell -ExecutionPolicy Bypass -File ./Start-HiveCLI.ps1",
    "start:tauri": "powershell -ExecutionPolicy Bypass -File ./Start-HiveCLI.ps1 -Tauri",
    "dev": "pnpm -r --parallel --if-present dev",
    "build": "pnpm -r --if-present build",
    "check": "pnpm -r --if-present check",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  }
}
